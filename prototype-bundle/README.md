# Mycelium prototype bundle

The GitHub Pages workflow concatenates the numbered `.b64` files, decodes the deterministic tarball, and publishes the extracted `site/` directory.

The bundle contains the readable static prototype: `index.html`, `demo.html`, `app.js`, and the CSS files. It has no runtime dependencies.
